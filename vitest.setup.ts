import { beforeAll, afterAll } from 'vitest';

import { vi } from 'vitest';

vi.mock('node_modules/vuetify/lib/**/*.css', () => ({}));

beforeAll(() => {
    // not used, yet
});

afterAll(() => {
    // not used, yet
});
